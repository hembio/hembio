// TODO: Add music genres
export enum MusicGenre {
  NONE,
}

// You can generate the below list with
// Object.keys(TitleGenres)
//   .filter((k) => isNaN(Number(k)))
//   .map((k) => k.replace(/_/g, "-").toLowerCase());
export const MusicGenreSlugs = {
  // TODO
};
