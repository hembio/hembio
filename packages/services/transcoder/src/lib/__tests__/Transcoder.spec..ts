// import { Transcoder } from '../Transcoder';

describe('Transcoder', () => {
  it('should initialize', () => {
    // const transcoder = new Transcoder();
    // expect(transcoder).toBeInstanceOf(Trancoder);
    expect(true).toBe(true);
  });
});
